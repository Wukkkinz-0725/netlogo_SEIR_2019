#! /usr/bin/env python
# import os
# import git
# import sys
# import subprocess

def main():
	# cmd = 'git diff --name-only --cached'
	# repo = git.Repo()
	# result = repo.git.diff('--name-only', '--cached')

	# # result = subprocess.check_output(cmd, shell=True)
	# files = result.split('\n')
	# # print('aaaa')
	# print(files)
	# # print(files)
	# a = files[0]
	# print(a)
	# print(os.path.splitext(a))

	# for file in files:
	# 	if file.endswith('py'):
	# 		print(file)
	# 		with open(file, 'r') as read_file:
	# 			tmp = read_file.read()
	# 			print(tmp)
	# print('AAAAAAAAA: %d', len(sys.argv))
	# print('BBBBBBBBB: %s' % str(sys.argv))
	# print('CCCCCCCCD')
	print('cyzpiggy!')
	print('tset pre-commit!')
	sys.exit(0)

if __name__ == "__main__":
	main()